import 'package:flutter/material.dart';

class AppColors {
  // 🎨 สีหลัก
  static const Color primary = Color.fromARGB(255, 255, 255, 237); // พื้นหลังหลัก (ครีม)
  static const Color secondary = Color.fromARGB(255, 77, 63, 44); // สีน้ำตาลเข้ม
  static const Color iconColor = Colors.black; // สีของไอคอน

  // 🎨 สีพื้นหลัง
  static const Color background = primary;
  static const Color darkBackground = secondary;

  // 🎨 สีของข้อความ
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.white;
}
